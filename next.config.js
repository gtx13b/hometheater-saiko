// next.config.js

/** @type {import('next').NextConfig} */
const nextConfig = {
  // 💡 ここに元の設定を記述します。
  // 例: experimental: { appDir: true }, 
  // 例: output: 'export',
  
  // もし Next.js 14系へダウングレードした場合、
  // Tailwind v3.x の設定は通常このファイルには必要ありません。

  // Tailwind v4の実験的な設定を使っていた場合は削除してください
};

module.exports = nextConfig;