/** @type {import('next').NextConfig} */
const nextConfig = {
  // ここに Next.js の設定オプションを記述します (例: reactStrictMode: true, など)
  // 設定がない場合は、そのまま空にしておきます
};

// 💡 修正点: CommonJSの module.exports から ES Moduleの export default に変更
export default nextConfig;